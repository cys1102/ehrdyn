# Scorer quickstart

The one-step and recursive probabilistic scorer remains exposed through the
existing scorer CLI and Python API. Public tests use only synthetic fixtures;
credentialed predictions must be aggregated before leaving the local workspace.
