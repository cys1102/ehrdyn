# Constructor quickstart

Set `AUTHORIZED_MIMICIV_3_1_ROOT` to an authorized local MIMIC-IV v3.1
directory, then run `ehrdyn-icu construct-ehr --output <private-dir>`. Use
`--aggregate-only` for a disclosure-safe receipt. The fitted route requires
the private role archives and `simulator_role_receipt.json`.
