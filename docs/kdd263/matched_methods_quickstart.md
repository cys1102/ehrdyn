# Matched methods and OPE quickstart

`ehrdyn-icu fitted-synthetic-smoke` runs the complete small-scale matched
workflow. The credentialed `fitted-simulator --mode evaluate` route runs the
same frozen 17 common methods, two fitted-only controls, and six estimators
inside the locally fitted simulator. Its result tables are local audit output,
not release assets.
