# Matched methods and OPE quickstart

`ehrdyn-icu fitted-synthetic-smoke` now runs the complete KDD267 small-scale
matched workflow. The credentialed `fitted-simulator --mode evaluate` route
runs the same exact 21 methods and six estimators inside the locally fitted
simulator. The fitted and controlled inventories are identical. Severity is
not a KDD267 method. Credentialed result tables are local audit output, not
release assets.
